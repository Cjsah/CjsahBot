dependencies {
    implementation(project(":core"))
}

archivesBaseName = "McCrawler"
version = plugin_version

processResources {
    filesMatching("plugin.json") {
        expand "version": project.version
    }
}